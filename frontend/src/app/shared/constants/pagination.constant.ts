import { Pagination } from '../models';

export const DEFAULT_FILTER_PAGINATION_LIMIT = 500;

export const pagination: Pagination = {
  // set values for pagination variables
  ITEMS_PER_PAGE: 20,
  CURRENT_PAGE: 0,
};
